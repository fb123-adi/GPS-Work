node_modules/

# Root-level patterns are anchored with a leading slash so they only apply at
# the repository root and don't swallow subprojects (e.g. the aurum/ app).
/dist/
/src/
/*.config.*
/tsconfig*
/package*
/postcss*
